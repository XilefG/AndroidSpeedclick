<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">
        <activity android:name=".PuzzleActivity"></activity>
        <activity android:name=".Tutorial" />
        <activity android:name=".findMatch" />
        <activity android:name=".onlineSelect" />
        <activity android:name=".ShowAbout" />
        <activity android:name=".ShowHighscores2" />
        <activity android:name=".ShowHelp" />
        <activity android:name=".ShowProfile" />
        <activity android:name=".ShowSettings" />
        <activity android:name=".ChooseCustomActivity" />
        <activity android:name=".DisplayGameOptions" />
        <activity android:name=".ShowHighscores" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>